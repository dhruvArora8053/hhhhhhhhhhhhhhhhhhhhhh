 .open .hidden-box {
        display: block;
      }
/* so if ope is present here on the parent element 'item', the the above selector will apply, because if open is not present on the parent element then ofcourse nothing is going to happen because above selector will not be applied anywhere */

 .open .hidden-box,
      .open .text {
        display: block;
      }